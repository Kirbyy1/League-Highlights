Run scripts\download_ffmpeg.ps1 (or setup.ps1) to place ffmpeg.exe and ffprobe.exe in tools\ffmpeg\bin.

The script downloads the Windows release essentials build published by gyan.dev, one of the Windows binary providers linked by FFmpeg's official download page.
Review the FFmpeg and build-provider licenses before distributing a bundled executable.
